package Clase_08;

/*===================================================================================
 *
 * 3)	Crear una función que sea un generador de contraseñas.
 *      Al llamarla debe devolver una contraseña (por ejemplo, puedes utilizar
 *           Math.random()
 *      para devolver un número aleatorio que podría ser una contraseña.
 *
 * ===================================================================================
 */
import java.util.Scanner;

/*public class Activ03_PasswordGenerator {
    public static void main (String[] args) {
        String nuevoPassword= getPasswordGenerator();
        System.out.println(nuevoPassword);
    }

    private static String getPasswordGenerator() {
        double aleatorio= Math.random()*1000000;
        long numeroAleatorio= Math.round(aleatorio);

        String password= "kjasAaks" + numeroAleatorio;

        return password;
    }

}
*/