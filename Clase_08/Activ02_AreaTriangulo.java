package Clase_08;
//import java.util.Scanner;

/*
----------------------------------------------------------------------
*
*    2)	Hacer una función que calcule el área de un triángulo.
*       Al llamarla debe devolver el área del triángulo.
* ----------------------------------------------------------------------
*/

/*
public class Activ02_AreaTriangulo {
    public static void main (String[] args) {

        float base= getUserData("Introduzca la longitud de la base del triangulo: ");
        float altura= getUserData("Introduzca la longitud de la altura del triangulo: ");

        float area= getTriangleArea(base, altura);

        System.out.println("El area del triangulo es: " + area);

    }

    private static float getTriangleArea(float base, float altura) {
        return (base*altura)/2;
    }

    private static float  getUserData(String msje) {
        System.out.println(msje);
        Scanner scanner = new Scanner(System.in);
        return scanner.nextFloat();
    }
}
*/